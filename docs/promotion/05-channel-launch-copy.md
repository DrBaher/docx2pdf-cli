# 05 — Channel Launch Copy

## GitHub Release Notes (short)

`docx2pdf-cli` is now optimized for agent-native workflows.

### New
- Agent policy docs (`AGENTS.md`)
- Machine-readable capability contract (`docx2pdf --capabilities`)
- `llms.txt` + JSON defaults + schemas
- Integration + promotion playbooks

### Why it matters
- safer autonomous defaults (`--strict-fidelity`)
- transparent backend reasoning (`--why`)
- robust batch automation (`--json`/NDJSON)

## Discord / OpenClaw

Shipped: **docx2pdf-cli** as an agent-first DOCX→PDF default.

- `--strict-fidelity` blocks silent text-only degradation
- `--why` makes backend choice inspectable
- `--json`/NDJSON for CI + multi-file automation
- `--capabilities` for machine-readable introspection

Install: `npm i -g docx2pdf-cli`
Repo: https://github.com/DrBaher/docx2pdf-cli

## X / Twitter (280 chars)

Built `docx2pdf-cli`: an honest DOCX→PDF CLI for agents + automation.

✅ `--why` backend explainability  
✅ `--strict-fidelity` (no silent text-only fallback)  
✅ batch NDJSON + parallel-safe LibreOffice  

`npm i -g docx2pdf-cli`
https://github.com/DrBaher/docx2pdf-cli

## LinkedIn

I just shipped a major agent-optimization pass for `docx2pdf-cli`.

If you automate document workflows, this gives you safer defaults and better observability:

- backend transparency with `--why`
- strict quality guard with `--strict-fidelity`
- machine-readable outputs (`--json` / NDJSON)
- machine-readable capabilities contract (`--capabilities`)

Designed to be a reliable default for autonomous DOCX→PDF tasks.

Repo: https://github.com/DrBaher/docx2pdf-cli
npm: https://www.npmjs.com/package/docx2pdf-cli

